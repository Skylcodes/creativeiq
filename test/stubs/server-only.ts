// Test-only stub for the `server-only` package, which throws when imported
// outside the Next.js server runtime. Aliased in vitest.config.ts so unit
// tests can import pipeline/report modules directly.
export {};
